base-font-size = 16
font-size(sizeWithPixel)
    size = unit(sizeWithPixel, '%')
    font-size: (size / base-font-size) * 100%

.clearfix
    &:before,&:after
        content: " "
        display: table
    &:after
        clear:both